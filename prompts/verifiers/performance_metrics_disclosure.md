# Performance Metrics Disclosure

Does the draft avoid publishing portfolio performance metrics or forward-looking statements without prior Marketing approval?
Return `pass` if no portfolio performance metrics or forward-looking statements are present.
Return `fail` if the draft contains portfolio performance metrics or forward-looking statements, which require Marketing/Megan V. sign-off before publishing.
Return only `pass` or `fail`, plus a short rationale.
